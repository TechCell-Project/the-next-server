export enum OrderStatusEnum {
    Pending = 'pending',
    Preparing = 'preparing',
    Shipping = 'shipping',
    Completed = 'completed',
    Canceled = 'canceled',
    Failed = 'failed',
}
