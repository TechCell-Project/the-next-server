export enum BlockAction {
    Block = 'block',
    Unblock = 'unblock',
}

export enum AddressType {
    Home = 'home',
    Office = 'office',
    Other = 'other',
}

export enum AuthProvider {
    Google = 'google',
    Facebook = 'facebook',
    Apple = 'apple',
    Email = 'email',
}
