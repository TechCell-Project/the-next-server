export enum GhnStatusEnum {
    enable_route = 1,
    disable_route = 2,
}

export enum GhnSupportTypeEnum {
    lock_route = 0,
    pickup_return = 1,
    delivery = 2,
    pickup_delivery_and_return = 3,
}
